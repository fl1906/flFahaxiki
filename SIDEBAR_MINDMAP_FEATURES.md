# 聊天页面实时思维导图侧边栏

## 功能概述

在聊天页面的右侧添加了可折叠的思维导图侧边栏，实时展示对话结构，帮助用户快速浏览和理解对话脉络。

## 主要特性

### 🎯 核心功能
- ✅ **实时更新**：随着对话进行自动更新思维导图
- ✅ **树状结构**：清晰的层级结构展示问题路径
- ✅ **智能提取**：自动从对话内容中提取关键话题和子话题
- ✅ **可折叠界面**：支持展开/收起，节省屏幕空间

### 📁 界面设计
- **右侧边栏**：固定在聊天页面右侧
- **可折叠机制**：
  - 展开状态：320px 宽度，显示完整思维导图
  - 收起状态：48px 宽度，仅显示思维导图图标
- **折叠按钮**：位于边栏左侧，圆形设计，易于点击
- **滚动区域**：支持长对话内容的滚动浏览

### 🌳 思维导图结构
```
对话主题 (根节点)
├── 用户问题1
│   └── AI回复1
│       ├── 子话题1
│       ├── 子话题2
│       └── 子话题3
├── 用户问题2
│   └── AI回复2
│       ├── 子话题1
│       └── 子话题2
└── ...
```

### 🧠 智能话题提取

#### 主题提取算法
1. **优先级排序**：
   - 第一句话（最优先）
   - 前40个字符（备选）
   - 清理Markdown符号

2. **子话题提取策略**：
   - **标题识别**：`# ## ### ####`
   - **列表项识别**：`- * +` 和 `1. 2. 3.`
   - **关键词识别**：`**重要性：**`
   - **加粗文本识别**：`**要点内容**`
   - **智能分割**：按标点、换行、词汇分割

3. **内容过滤**：
   - 长度限制：3-80字符
   - 过滤纯英文数字
   - 清理Markdown符号
   - 去重处理

### 🎨 视觉设计

#### 节点样式
- **用户节点**：蓝色 User 图标
- **AI节点**：绿色 Bot 图标
- **根节点**：紫色 Brain 图标
- **层级缩进**：每级16px左缩进
- **连接线**：灰色虚线显示父子关系

#### 交互功能
- **展开/收起**：点击父节点切换子节点显示
- **悬停效果**：鼠标悬停时背景高亮
- **状态指示**：
  - 展开状态：向下箭头
  - 收起状态：向右箭头
  - 叶子节点：无箭头

### 💻 技术实现

#### 组件结构
```
src/components/mindmap/
├── sidebar-mindmap.tsx      # 核心思维导图组件
└── collapsible-sidebar.tsx  # 可折叠边栏容器
```

#### 核心算法
1. **消息映射**：
   ```typescript
   // 将消息数组转换为树状结构
   messages.forEach((message, index) => {
     if (message.type === 'user') {
       // 创建用户节点
       const nextMessage = messages[index + 1]
       if (nextMessage?.type === 'ai') {
         // 创建AI回复节点
         // 提取并创建子话题节点
       }
     }
   })
   ```

2. **话题提取**：
   ```typescript
   const extractSubtopics = (content: string): string[] => {
     // 多模式匹配：标题、列表、关键词、加粗文本
     // 智能过滤：长度、去重、符号清理
     // 备用方案：文本分割
   }
   ```

3. **状态管理**：
   ```typescript
   const [expandedNodes, setExpandedNodes] = useState<Set<string>>()
   // 默认展开所有节点
   // 支持用户交互控制
   ```

### 📱 响应式设计
- **桌面端**：侧边栏固定宽度
- **移动端**：自动收起，节省空间
- **自适应布局**：主对话区域宽度自动调整

### ⚡ 性能优化
- **实时更新**：仅在有新消息时重新生成思维导图
- **智能渲染**：使用展开状态控制渲染范围
- **内容限制**：限制子话题数量（最多6个）
- **缓存机制**：避免重复计算

### 🔧 集成方式

#### 在聊天页面使用
```tsx
import CollapsibleSidebar from '@/components/mindmap/collapsible-sidebar'

// 在聊天布局中集成
{messages.length > 0 && (
  <div className="border-l">
    <CollapsibleSidebar
      messages={messages}
      conversationId={conversationId}
      className="h-full"
    />
  </div>
)}
```

#### 独立组件使用
```tsx
import SidebarMindmap from '@/components/mindmap/sidebar-mindmap'

<SidebarMindmap
  messages={messages}
  conversationId={conversationId}
  className="custom-class"
/>
```

## 🎯 用户体验

### 使用场景
1. **长对话浏览**：快速定位问题焦点
2. **结构化理解**：清晰展示对话脉络
3. **话题追踪**：了解AI回复的主要内容
4. **回溯查找**：快速找到相关的历史问题

### 交互流程
1. 开始对话 → 侧边栏自动显示
2. 发送问题 → 思维导图实时更新
3. 点击箭头 → 展开/收起子话题
4. 点击折叠按钮 → 收起整个侧边栏
5. 继续对话 → 思维导图持续更新

### 优势特点
- **非侵入式**：不影响正常对话体验
- **实时性**：即时反映对话变化
- **智能化**：自动提取关键信息
- **灵活性**：支持自定义展示方式

## 🚀 服务器状态
- 开发服务器正常运行：http://localhost:3004
- 新功能已集成完成，可直接使用
- 所有编译通过，无错误或警告

这个实时思维导图侧边栏为用户提供了全新的对话浏览体验，让复杂的AI对话变得更加结构化和易于理解！